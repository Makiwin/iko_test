import { RelatedProductsPipe } from './related-products.pipe';

describe('RelatedProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new RelatedProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
